{
  "name": "afm-backend",
  "version": "1.0.0",
  "description": "Apna Fashion Mart API Lambda functions",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "zip -r function.zip . -x '*.git*' -x 'node_modules/.bin/*'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/rds-signer": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "aws-jwt-verify": "^4.0.0",
    "pg": "^8.11.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "jest": "^29.0.0"
  }
}

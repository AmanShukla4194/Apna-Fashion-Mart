# ============================================================
# Cloudflare Pages Configuration — Apna Fashion Mart
# Docs: https://developers.cloudflare.com/pages/
# ============================================================

name = "apna-fashion-mart"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Pages build output directory (built by @cloudflare/next-on-pages)
pages_build_output_dir = "apps/web/.vercel/output/static"

# Environment variables for production
# Secrets (SUPABASE_SERVICE_ROLE_KEY, RAZORPAY_KEY_SECRET, etc.)
# are set via Cloudflare Dashboard > Pages > Settings > Environment Variables
# or via: wrangler pages secret put SECRET_NAME

[env.production]
# Public vars (safe to commit)
# Sensitive vars must be set in Cloudflare Dashboard, NOT here

[env.preview]
# Same as production but for preview deployments (PRs)

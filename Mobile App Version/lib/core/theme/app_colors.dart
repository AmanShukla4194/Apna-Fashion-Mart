import 'package:flutter/material.dart';

/// AFM brand colour tokens.
abstract final class AppColors {
  /// Navy 800 — primary brand dark blue.
  static const Color navy800 = Color(0xFF001F3F);

  /// Magenta 600 — primary accent / CTA colour.
  static const Color magenta600 = Color(0xFFFF1493);

  /// Gold 500 — premium / highlight colour.
  static const Color gold500 = Color(0xFFC9A24A);
}
